rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regra padrão - ajuste conforme necessário
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
